public class CourseBuildingForm
{
    protected Course createCourse(String courseType)
    {
        //Course newCourse = null;

        Course newCourse = null;
        CourseFactory CourseFactory = new CourseFactory("Self-Pace");
        //newCourse.setName("Object-Oriented-Design");
        /* Modify so that we have one factory for all courses
            * Inside each factory, check what type of course, instructor, Student
            we want to make
        CSCourseFactory csCourseFactory = new CSCourseFactory();

        */
        return newCourse;
    }
}
